
# Dolt database files (added by bd init)
.dolt/
*.db

# Gas Town (added by gt)
.runtime/
.claude/commands/
.logs/
CLAUDE.md

# Node
node_modules/
# dist/ is intentionally committed so the package can be used as a GitHub dependency
.cache/

# Env
.env
.env.local
.env.*.local

# Editor
.DS_Store
*.tgz
.land-worktree/
